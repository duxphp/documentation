---
title: '页面用法'
description: 'DuxCMS Next 使用 latte 作为模板引擎，页面基础用法。'
---

## 列表

### 路由规则
url `{$domain}/{模块名}`

### 页面使用
在模板目录下添加 `{模块名}.latte` 文件

## 详情

### 路由规则
url `{$domain}/{模块名}/{id}`

### 页面使用
在模板目录下添加 `{模块名}-info.latte` 文件
